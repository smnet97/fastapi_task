version: '3'

services:
  task:
    build: .
    command: sh -c "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000"
    env_file:
      - .env
    ports:
      - 8000:8000
    volumes:
      - .:/fastapi_task